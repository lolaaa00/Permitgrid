# Copy to .env.local and fill in after deploying contracts/permitgrid.py.
# Until NEXT_PUBLIC_CONTRACT_ADDRESS is set, every page shows a clear
# "not configured" state instead of attempting live calls.

NEXT_PUBLIC_CONTRACT_ADDRESS=
NEXT_PUBLIC_RPC_URL=https://studio.genlayer.com/api
